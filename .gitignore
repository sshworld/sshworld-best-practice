# 민감 정보 — 절대 commit 금지
.env
.env.local
.env.*.local
*.pem
*.key
*.p12

# 로컬 권한 오버라이드 (개인 세션 한정)
.claude/settings.local.json

# 백업
*.bak
*.bak.*

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo

# tmux pane dispatcher artifacts
.worktrees/

# Claude Code built-in subagent worktrees (agent-*/worktree-agent-* 브랜치)
.claude/worktrees/

# dispatch spec 잔여물 — 매 dispatch 마다 생기는 일회성 산출물
# 이미 추적 중인 파일(commit-advisor-gate.spec.md 등)은 영향 없음
.claude/specs/*.spec.md

# Node / Python (있다면)
node_modules/
__pycache__/
*.pyc
.venv/
venv/
